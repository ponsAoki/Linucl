import { FC, memo } from "react";

export const Footer: FC = memo(() => {
  return(
    <div className="mx-auto text-center bg-black">
      <small className="text-white">&copy; Linucl</small>
    </div>
  )
})
